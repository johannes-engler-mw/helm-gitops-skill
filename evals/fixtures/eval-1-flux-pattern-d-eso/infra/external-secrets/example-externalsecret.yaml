apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: example-app-credentials
  namespace: example-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: example-app-credentials
  data:
    - secretKey: password
      remoteRef:
        key: example-app/credentials
        property: password
