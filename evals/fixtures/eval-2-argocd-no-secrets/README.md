# GitOps repo (test fixture)

This repository holds our ArgoCD applications. We bootstrap apps through the `argocd/apps/` directory.

We do not yet have a secrets-management solution installed in the cluster.
